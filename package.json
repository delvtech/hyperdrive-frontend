{
  "name": "hyperdrive-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=./packages/*",
    "dev": "turbo run dev --parallel --filter=./apps/*",
    "lint": "turbo run lint",
    "lint:packages": "turbo run lint --filter=./packages/*",
    "test": "turbo run test --filter=./packages/*",
    "test:run": "turbo run test:run --filter=./packages/*",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "postinstall": "husky install",
    "typecheck": "turbo run typecheck",
    "start:docs": "turbo run start:docs --filter=./apps/hyperdrive-sdk-docs --force"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "latest",
    "turbo": "latest",
    "typescript": "^5.0.2"
  },
  "resolutions": {
    "typescript": "^5.0.2",
    "viem": "^1.10.14"
  },
  "lint-staged": {
    "{apps,packages}/**/*.{gql,graphql,js,jsx,ts,tsx,json}": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.18"
}
