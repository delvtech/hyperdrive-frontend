node_modules

# Ignore temporary clones of the hyperdrive repo used by the build script
hyperdrive-temp.*

# Ignore the src folder which is generated by the build script
src

# Save the dist folder which requires forge to build
!dist