# Pool Config

Documentation for the pool config, what you can provide and what it returns. TODO

```ts {3}
const pool = configurePoolActions({hyperdriveAddress, hyperdriveMathAddress, queryClient})

const { baseToken, initialSharePrice } = await pool.getPoolConfig()
```