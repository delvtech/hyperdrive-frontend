# Configure Pool

TODO

```ts
const pool = configurePoolActions({hyperdriveAddress, hyperdriveMathAddress, queryClient})

const poolConfig = pool.getPoolConfig()
const poolInfo = pool.getPoolInfo()
const fixedRate = pool.getFixedRate()
```