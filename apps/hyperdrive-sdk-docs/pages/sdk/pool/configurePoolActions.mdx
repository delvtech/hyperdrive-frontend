# Configure Pool

```ts {1}
const pool = configurePoolActions({hyperdriveAddress, hyperdriveMathAddress, queryClient})

const poolConfig = await pool.getPoolConfig()
const poolInfo = await pool.getPoolInfo()
const fixedRate = await pool.getFixedRate()
```