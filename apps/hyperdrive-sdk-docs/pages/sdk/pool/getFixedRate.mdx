# Fixed Rate

Documentation for the fixed rate, what you can provide and what it returns. TODO

```ts {3}
const pool = configurePoolActions({hyperdriveAddress, hyperdriveMathAddress, queryClient})

const { apr, formatted } = await pool.getFixedRate()
```