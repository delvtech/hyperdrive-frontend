# Pool Info

Documentation for the pool info, what you can provide and what it returns. TODO

```ts {3}
const pool = configurePoolActions({hyperdriveAddress, hyperdriveMathAddress, queryClient})

const { shareReserves, bondReserves } = await pool.getPoolInfo()
```
