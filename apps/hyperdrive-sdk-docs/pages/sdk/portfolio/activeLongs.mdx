# Active Longs

Documentation for active longs, what you can provide and what it returns. TODO

```ts {3}
const portfolio = configurePortfolioActions({hyperdriveAddress, hyperdriveMathAddress, queryClient})

const activeLongs = await portfolio.getActiveLongs()
const { bondAmount, baseAmountPaid } = activeLongs.length && activeLongs[0]
```