# Configure Portfolio

TODO

```ts
const portfolio = configurePortfolioActions({hyperdriveAddress, hyperdriveMathAddress, queryClient})

const activeLongs = portfolio.getActiveLongs()
const closedLongs = portfolio.getClosedLongs()
const activeShorts = portfolio.getActiveShorts()
const closedShorts = portfolio.getClosedShorts()
```